const api = {
    auth: {
        signIn: '/login/'
    },
    logistics: {
        base: "logistics",
        commodities: "/commodities"
    }
};

export const BACKEND = {
    base: "http://localhost:8000/",
    api: api,
};
